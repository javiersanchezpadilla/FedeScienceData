""" Genera un conjunto de 4 subgráficos (2 filas por 2 columnas). 
    Asignalos a dos variables (fig y axs), para poder almacenar allí el gráfico."""

import matplotlib.pyplot as plt 

fig, axs = plt.subplots(nrows=2, ncols=2)
plt.show()
